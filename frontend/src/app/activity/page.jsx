import PlaceholderPage from '@/components/shared/PlaceholderPage';

export default function ActivityPage() {
  return (
    <PlaceholderPage
      title="Activity feed"
      description="This page prevents broken links from the dashboard and gives you a landing spot for the organization-wide activity stream."
      primaryHref="/dashboard"
      primaryLabel="Go to dashboard"
    />
  );
}
