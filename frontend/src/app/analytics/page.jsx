import PlaceholderPage from '@/components/shared/PlaceholderPage';

export default function AnalyticsPage() {
  return (
    <PlaceholderPage
      title="Analytics reports"
      description="The analytics route is now wired so the sidebar navigation works. The dashboard metrics remain available from the main dashboard screen."
      primaryHref="/dashboard"
      primaryLabel="Open dashboard analytics"
    />
  );
}
