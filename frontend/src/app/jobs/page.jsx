import PlaceholderPage from '@/components/shared/PlaceholderPage';

export default function JobsPage() {
  return (
    <PlaceholderPage
      title="Jobs workspace"
      description="The jobs route now exists so the main navigation no longer 404s. You can extend it with full job CRUD on top of the working backend jobs API."
      primaryHref="/dashboard"
      primaryLabel="View dashboard"
    />
  );
}
